name: Reusable Workflow Caller

on:
  pull_request:
  workflow_dispatch:

jobs:
  call-reusable-workflow:
    if: ${{ false }} # TODO: replace `uses:` with your actual reusable workflow ref, then remove this guard
    uses: owner/repo/.github/workflows/file.yml@ref
